import React from 'react';
import PropTypes from 'prop-types';

const ASRLabellingEdit = props => {
    return (
        <React.Fragment>
            ASRLabellingEdit
        </React.Fragment>
    );
};

ASRLabellingEdit.propTypes = {
    
};

export default ASRLabellingEdit;
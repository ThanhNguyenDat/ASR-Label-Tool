import React from 'react';
import PropTypes from 'prop-types';

function FaceIDAdmin ( props ) {
    const columns = []
    const dataDataTable = []

    return (
        <div>
            <h1>Admin</h1>
        </div>
    );
};

FaceIDAdmin.propTypes = {
    
};

export default FaceIDAdmin;
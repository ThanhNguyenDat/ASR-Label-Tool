import React from 'react';
import PropTypes from 'prop-types';

import TextEditor from "../../components/textEditor/TextEditor";

const TestTextEditor = props => {
    return (
        <div>
            <TextEditor />
        </div>
    );
};

TestTextEditor.propTypes = {
    
};

export default TestTextEditor;
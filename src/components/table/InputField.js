import React from 'react';
import PropTypes from 'prop-types';

function InputField({data, ...rest}) {

    return (
        <div>
            
        </div>
    );
};

InputField.propTypes = {
    
};

export default InputField;
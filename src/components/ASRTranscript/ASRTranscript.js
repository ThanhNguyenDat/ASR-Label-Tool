import React from 'react';
import PropTypes from 'prop-types';

const ASRTranscript = props => {
    return (
        <div>
            
        </div>
    );
};

ASRTranscript.propTypes = {
    
};

export default ASRTranscript;